# Vite build-time environment variables
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_API_BASE_URL=

# Authentication mode: 'auto' (detect WIA first, fallback to Supabase), 
# 'wia' (force WIA only), 'supabase' (skip WIA, use Supabase only)
# Default: auto
VITE_AUTH_MODE=auto

# Add additional API endpoints/keys as needed, prefixed with VITE_
