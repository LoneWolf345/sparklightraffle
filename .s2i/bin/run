#!/bin/bash

# Custom S2I run script for Vite React application
# This script runs when the container starts

set -e

echo "==> Starting application server on port ${PORT:-8080}..."

# Serve the built static files using serve package
exec ./node_modules/.bin/serve -s dist -l ${PORT:-8080}
